#데이터 구조 소개

#데이터 유형, 인덱싱 및 축 레이블 지정, 정렬에 대한 기본동작은 모든개체에 적용된다.
#시작하려면 NumPy를 가져오고 pandas를 네임 스페이스에 로드힌다.
import numpy as np
import pandas as pd

#명심해야 할 기본원칙은 다음과 같다.
#   - 데이터 정렬은 내재적 이다. 
#   - 라벨과 데이터 간의 연결은 사용자가 명시적으로 수행하지 않는 한 끊어지지 않는다.
