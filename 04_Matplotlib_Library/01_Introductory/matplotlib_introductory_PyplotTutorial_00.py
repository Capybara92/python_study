#Pyplot 튜토리얼
#Pyplot소개
#matplotlib.pyplot은 matplotlib이 MATLAB처럼 작동하도록하는 함수 모음이다. 
#각각의 pyplot함수는 특징을 조금 변경한다. 예를들어
#creates a figure, creates a plotting area in a figure, plots some lines in a plotting area, decorates the plot with labels, etc.
#the current figure and plotting area, and the plotting functions are directed to the current axes를 유지하기 위해서
#matplotlib.pyplot 다양한 상태가 함수 호출에 걸쳐 보존된다. 

#pyplot API는 일반적으로 객체 지향 API보다 유연성이 떨어진다.
#여기에서 볼 수있는 대부분의 함수 호출은 Axes객체의 메서드로 호출 될 수도 있다.
